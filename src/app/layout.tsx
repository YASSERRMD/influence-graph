import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Influence Graph Platform",
  description: "Model and visualize dynamic influence relationships inside an organization with real-time updates, analytics, and interactive graph visualization.",
  keywords: ["Influence Graph", "Organization Analytics", "Network Analysis", "Real-time Updates", "Next.js"],
  authors: [{ name: "Influence Graph Team" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "Influence Graph Platform",
    description: "Model and visualize dynamic influence relationships inside organizations",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
